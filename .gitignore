node_modules/
dist/
src-tauri/target/
.env
.env.*
!.env.example
*.log
*.local
.DS_Store
.playwright-mcp/
zeus-*-fit.png
zeus-main-screen.png
zeus_visual_specs.zip
# One-shot migration scripts — kept locally for reproducibility, not part
# of the shipping code. Re-run with `python3 scripts/<name>.py` if a
# future migration needs the same approach.
scripts/
